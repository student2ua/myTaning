<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!--...-->


    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
            <resource>
                <directory>src/main/html</directory>
                <filtering>true</filtering>
                <targetPath>../</targetPath>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>keytool-maven-plugin</artifactId>
                <version>1.0</version>
                <executions>
                    <execution>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>genkey</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <alias>${keystore.alias}</alias>
                    <keystore>${keystore.location}</keystore>
                    <dname>cn=Brent Atkinson, ou=OpenXData, o=UIB, c=NO</dname>
                    <keypass>${keystore.password}</keypass>
                    <storepass>${keystore.password}</storepass>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.2.1</version>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/all-in-one.xml</descriptor>
                    </descriptors>
                    <archive>
                        <manifest>
                            <mainClass>${application.mainclass}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.github.batkinson.plugins</groupId>
                <artifactId>maven-pack200-plugin</artifactId>
                <version>1.0.1</version>
                <executions>
                    <execution>
                        <id>normalize-jars</id>
                        <phase>package</phase>
                        <goals>
                            <goal>pack</goal>
                        </goals>
                        <configuration>
                            <normalizeOnly>true</normalizeOnly>
                        </configuration>
                    </execution>
                    <execution>
                        <id>pack-jars</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>pack</goal>
                        </goals>
                        <configuration>
                            <normalizeOnly>false</normalizeOnly>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <processMainArtifact>false</processMainArtifact>
                    <includeClassifiers>
                        <includeClassifier>all-in-one</includeClassifier>
                    </includeClassifiers>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-jarsigner-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>sign</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <alias>${keystore.alias}</alias>
                    <keystore>${keystore.location}</keystore>
                    <keypass>${keystore.password}</keypass>
                    <storepass>${keystore.password}</storepass>
                    <processMainArtifact>false</processMainArtifact>
                    <processAttachedArtifacts>true</processAttachedArtifacts>
                    <includeClassifiers>
                        <includeClassifier>all-in-one</includeClassifier>
                    </includeClassifiers>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <properties>
        <keystore.alias>keys</keystore.alias>
        <keystore.location>${project.build.directory}/location-keystore</keystore.location>
        <keystore.password>pass</keystore.password>
        <application.mainclass>org.....Main</application.mainclass>
        <application.appclass>org.....DesignerApp</application.appclass>
    </properties>

</project>